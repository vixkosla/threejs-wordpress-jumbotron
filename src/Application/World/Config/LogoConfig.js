/**
 * Конфигурация логотипов
 * Все параметры вывески-логотипов (два логотипа)
 */

export const LOGO_CONFIG = {
  // === Логотип 1 (основной) ===
  logo1: {
    // === Позиция ===
    position: {
      x: -0.25, // Позиция по X
      y: -0.6, // Позиция по Y
      z: 0, // Позиция по Z
    },

    // === Поворот (в радианах) ===
    rotation: {
      x: 0, // Наклон вперёд/назад
      y: -2 * Math.PI, // Поворот вокруг Y
      z: -Math.PI / 2, // Наклон влево/вправо
    },

    // === Размер ===
    size: {
      width: 1.4 * 2, // Ширина плоскости
      height: 0.4 * 2, // Высота плоскости
    },

    // === Материал ===
    material: {
      color: 0xffffff, // Цвет логотипа
      opacity: 0.0515, // Прозрачность (0-1)
      transparent: true,
    },
  },

  // === Логотип 2 (дубликат у T-блока, ярче) ===
  logo2: {
    // === Позиция ===
    position: {
      x: -0.2, // Позиция по X
      y: 1.4, // Позиция по Y
      z: 0, // Позиция по Z
    },

    // === Поворот (в радианах) ===
    rotation: {
      x: 0, // Наклон вперёд/назад
      y: -2 * Math.PI, // Поворот вокруг Y
      z: -Math.PI / 2, // Наклон влево/вправо
    },

    // === Размер ===
    size: {
      width: 1.4 * 2, // Ширина плоскости
      height: 0.4 * 2, // Высота плоскости
    },

    // === Материал ===
    material: {
      color: 0xffffff, // Цвет логотипа (ярче)
      opacity: 0.1, // Прозрачность (ярче чем logo1)
      transparent: true,
    },
  },
};

/**
 * Предустановки позиций для разных мест
 */
export const LOGO_PRESETS = {
  // У T-блока (текущая)
  tBlock: {
    position: { x: -0.3, y: 1.0, z: 1.5 },
    rotation: { x: 0, y: -Math.PI / 2, z: 0 },
  },

  // У блока 0 (фиолетовый, левый нижний)
  block0: {
    position: { x: -2.0, y: 1.8, z: 1.2 },
    rotation: { x: 0, y: 0, z: 0 },
  },

  // Спереди композиции (по центру)
  frontCenter: {
    position: { x: 0, y: 1.5, z: 2.5 },
    rotation: { x: 0, y: 0, z: 0 },
  },

  // Справа от композиции (вертикально)
  rightSide: {
    position: { x: 1.5, y: 1.0, z: 0 },
    rotation: { x: 0, y: -Math.PI / 2, z: 0 },
  },

  // Слева от композиции (вертикально)
  leftSide: {
    position: { x: -2.5, y: 1.0, z: 0 },
    rotation: { x: 0, y: Math.PI / 2, z: 0 },
  },
};

export default LOGO_CONFIG;
