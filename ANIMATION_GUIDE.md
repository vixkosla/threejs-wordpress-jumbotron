# 📖 Руководство по настройке анимации блоков

## 📁 Структура файлов

```
src/Application/World/Config/
├── AnimationConfig.js    ← ГЛАВНЫЙ файл настроек анимации
├── HexagonPositions.js   ← Настройки света и позиций шестиугольников
└── LogoConfig.js         ← Настройки логотипов
```

## ⚡ Быстрая настройка

### 1. Изменить чувствительность к мыши/гироскопу

Откройте `AnimationConfig.js`, найдите секцию `input`:

```javascript
input: {
  deadZone: 0.05,        // Мёртвая зона (увеличьте если слишком чувствительно)
  cooldownTime: 80,      // Задержка между срабатываниями (мс)
  mouseSensitivity: 1.0, // Общая чувствительность
},
```

**Проблема:** Блоки слишком сильно двигаются от малейшего движения  
**Решение:** Увеличьте `deadZone` до `0.1` или уменьшите `mouseSensitivity` до `0.5`

---

### 2. Изменить физику движения

Секция `physics`:

```javascript
physics: {
  acceleration: 0.25,    // Как быстро блоки ускоряются
  friction: 0.92,        // Как быстро останавливаются
  returnSpeed: 0.08,     // Как быстро возвращаются на место
},
```

**Хочется более плавной анимации:**
- Увеличьте `friction` до `0.95-0.98`
- Уменьшите `returnSpeed` до `0.03-0.05`

**Хочется более резкой анимации:**
- Увеличьте `acceleration` до `0.3-0.5`
- Увеличьте `returnSpeed` до `0.1-0.15`

---

### 3. Изменить амплитуду движения

Секция `limits`:

```javascript
limits: {
  amplitudeMultiplier: 2.5,  // ОБЩИЙ множитель амплитуды
  blockMaxOffset: 0.15,      // Макс. смещение блока
  groupMaxOffset: 0.3,       // Макс. смещение группы
},
```

**Блоки двигаются слишком сильно:**
- Уменьшите `amplitudeMultiplier` до `1.0-1.5`
- Уменьшите `blockMaxOffset` до `0.05-0.1`

**Блоки почти не двигаются:**
- Увеличьте `amplitudeMultiplier` до `3.0-4.0`
- Увеличьте `blockMaxOffset` до `0.2-0.3`

---

### 4. Настроить "живую" анимацию (случайные импульсы)

Секция `impulse`:

```javascript
impulse: {
  enabled: true,           // Включить/выключить
  baseTime: 1000,          // Базовое время (мс)
  chancePerFrame: 0.08,    // Шанс импульса
  force: 0.015,            // Сила импульса
},
```

**Блоки слишком часто двигаются сами по себе:**
- Уменьшите `chancePerFrame` до `0.02-0.05`
- Увеличьте `baseTime` до `2000-3000`

**Хочется больше "жизни":**
- Увеличьте `chancePerFrame` до `0.1-0.15`
- Увеличьте `force` до `0.02-0.03`

**Отключить живую анимацию:**
```javascript
enabled: false,
```

---

### 5. Настроить левитацию T-блока

Секция `tBlock`:

```javascript
tBlock: {
  floatSpeed: 1.5,         // Скорость левитации
  floatAmplitude: 0.05,    // Амплитуда (высота)
},
```

**T-блок слишком сильно плавает:**
- Уменьшите `floatAmplitude` до `0.02-0.03`

**Хочется медленнее:**
- Уменьшите `floatSpeed` до `0.5-1.0`

---

### 6. Изменить векторы направления блоков

Каждый блок двигается вдоль своего вектора:

```javascript
// Блок 0 (фиолетовый, левый нижний)
pushVector0: { x: 0, y: -0.7, z: -1 },
```

**Что означают значения:**
- `x`: движение влево (-) / вправо (+)
- `y`: движение вниз (-) / вверх (+)
- `z`: движение от камеры (-) / к камере (+)

**Пример:** Хочется чтобы блок 0 двигался вправо при движении мыши вправо:
```javascript
pushVector0: { x: 1, y: 0, z: 0 },
```

---

## 🎯 Примеры пресетов

### Пресет 1: "Очень плавная анимация"

```javascript
physics: {
  acceleration: 0.15,
  friction: 0.96,
  returnSpeed: 0.04,
},
limits: {
  amplitudeMultiplier: 1.5,
  blockMaxOffset: 0.1,
},
impulse: {
  enabled: true,
  force: 0.008,
},
```

---

### Пресет 2: "Очень отзывчивая анимация"

```javascript
physics: {
  acceleration: 0.4,
  friction: 0.88,
  returnSpeed: 0.12,
},
limits: {
  amplitudeMultiplier: 3.5,
  blockMaxOffset: 0.25,
},
impulse: {
  enabled: false,  // Отключаем живую анимацию
},
```

---

### Пресет 3: "Минимальная анимация" (почти статично)

```javascript
physics: {
  acceleration: 0.1,
  friction: 0.98,
  returnSpeed: 0.02,
},
limits: {
  amplitudeMultiplier: 0.5,
  blockMaxOffset: 0.05,
  groupMaxOffset: 0.1,
},
impulse: {
  enabled: false,
},
```

---

## 🔧 Отладка

### Включить логирование

В `MyModel.js` найдите метод `updateMousePosition` и раскомментируйте:

```javascript
console.log('Mouse delta:', this.mouseDelta.x, this.mouseDelta.y);
console.log('Block velocity:', this.blockVelocities[0]);
```

### Проверить текущие значения

Откройте консоль браузера и введите:

```javascript
// Проверка конфигурации
console.log(window.APP_ANIMATION_CONFIG);
```

---

## 📱 Мобильная версия

Настройки гироскопа находятся в `Application.js`:

```javascript
// Чувствительность гироскопа
this.gyroSensitivity = 0.012;  // Увеличьте для большей чувствительности
```

**Гироскоп слишком чувствительный:**
- Уменьшите до `0.005-0.008`

**Гироскоп почти не реагирует:**
- Увеличьте до `0.015-0.02`

---

## ❓ Частые вопросы

### Q: Блоки дергаются при движении
**A:** Увеличьте `cooldownTime` до `100-150` мс или увеличьте `deadZone`.

### Q: Блоки не возвращаются на место
**A:** Увеличьте `returnSpeed` до `0.1-0.15`.

### Q: Анимация слишком медленная
**A:** Увеличьте `acceleration` и `amplitudeMultiplier`.

### Q: Блоки улетают за экран
**A:** Уменьшите `blockMaxOffset` и `groupMaxOffset`.

### Q: Живая анимация слишком заметная
**A:** Уменьшите `impulse.force` до `0.005-0.01` или отключите совсем.

---

## 📞 Поддержка

Все изменения применяются сразу после перезагрузки страницы.

**Сохраняйте оригинальные значения** чтобы можно было вернуть назад!
