# Сессия разработки: G Material + гидравлическая анимация блоков

## Дата
21 февраля 2026

## Цель
Создать материал с эффектом frosted glass (матовое стекло) для G-блоков с transmission и плавной анимацией от движения мыши.

---

## 📦 Зависимости

```bash
npm install lil-gui          # GUI для отладки
npm install postprocessing   # (удалено, не нужно)
npm install gsap             # Плавные анимации
```

---

## 🎨 Структура проекта

```
src/Application/World/
├── Materials/
│   ├── GMaterial.js         ← MeshPhysicalMaterial с transmission
│   └── TMaterial.js         ← (не используется)
├── Config/
│   └── HexagonPositions.js  ← Позиции шестиугольников-источников
├── BacklightHexagon.js      ← Источник света (без геометрии)
├── PositionMarker.js        ← Чёрная точка-маркер позиции
├── SettingsManager.js       ← Сохранение настроек в localStorage
├── HexagonsControlPanel.js  ← Единая панель управления 3 шестиугольниками
├── MyModel.js               ← Блоки G и T с анимацией
└── World.js                 ← Мир + свет
```

---

## 🔷 G Material (MeshPhysicalMaterial)

### Параметры transmission:
- `transmission: 0.92` — прозрачность с преломлением
- `thickness: 0.6` — толщина для преломления
- `roughness: 0.35` — шероховатость (матовость)
- `ior: 1.45` — индекс преломления

### GUI панель "G Material" (правый верхний угол):
- **Основное**: transmission, thickness, roughness, IOR
- **Цвет**: color picker
- **Поверхность**: metalness, reflectivity, clearcoat, coat roughness
- **Окружение**: envMapIntensity, attenuation color/distance

---

## 🎨 Цвета блоков

```js
const COLOR_PURPLE = 0xb8b8ff;  // лёгкий фиолетово-синий (пастельный)
const COLOR_LIME = 0xa3e635;    // мягкий салатовый (свежее яблоко)
const COLOR_GLASS = 0xf8f9fa;   // стеклянный белый (полупрозрачный пластик)
```

### Раскраска G-блоков:
| № | Позиция | Цвет |
|---|---------|------|
| 1 | [-2.0, 1, 0.5] | 🟣 Фиолетово-синий |
| 2 | [-0.5, 2, 2] | ⚪ Белый |
| 3 | [0, 0.5, 2] | ⚪ Белый |
| 4 | [-2, 2.5, 1] | 🟣 Фиолетово-синий |
| 5 | [-2, 2.5, 2.5] | 🟢 Салатовый (единственный) |
| 6 | [-3, 1, 1] | ⚪ Белый |

**T-блок**: [-0.5, 1.5, 1.0] — белый

---

## 💡 Освещение (3 шестиугольника)

### Позиции:
1. **Front**: (-4, -1.5, 4) — ближе к камере
2. **Left**: (8, -3, 8) — слева
3. **Right**: (-8, -3, 8) — справа

### GUI панель "Hexagons Control" (левый верхний угол):
- **🌍 Орбиты**: Радиус, Azimuth, Elevation для каждого
- **💡 Интенсивность света**: RectArea, Back Light для каждого
- **💾 Сохранение**: сохранить/загрузить настройки
- **⚙️ Сброс позиций**: для каждого шестиугольника

### Маркеры:
- Чёрные сферы (радиус 0.15) показывают позицию источника
- Не влияют на освещение

---

## 🌊 Гидравлическая анимация блоков

### Физика с ускорением и трением
Реализована полноценная физика: ускорение → скорость → трение → возврат.

### Параметры:
```js
acceleration: 0.15,        // Ускорение от движения мыши
friction: 0.92,            // Трение (замедление, 0.90-0.96 для плавности)
returnSpeed: 0.08,         // Скорость возврата к позиции покоя
amplitudeMultiplier: 1.5,  // Амплитуда (очень мягко)
deadZone: 0.05,            // Мёртвая зона мыши
staggerDelays: [0, 0.05, 0.1, 0.15, 0.2, 0.25] // Задержки
cooldownTime: 80,          // Защита от дерганий (мин. мс между срабатываниями)
```

### "Живая" анимация (эффект дыхания):
```js
randomImpulseChance: 0.02,     // Шанс импульса каждый кадр (2%)
randomImpulseMinTime: 2000,    // Мин. время между импульсами (мс)
randomImpulseMaxTime: 5000,    // Макс. время между импульсами (мс)
randomImpulseForce: 0.3,       // Сила импульса
```
Случайные блоки получают небольшие импульсы в своём направлении, создавая эффект "живой" композиции.

### Векторы направлений (2D проекция от движения мыши):
| Блок | Вектор | Направление |
|------|--------|-------------|
| 0 | (0, -1) | Движение мыши вниз = блок к зрителю (-Z) 🟣 |
| 1 | (0.5, 1) | Движение мыши вверх-вправо |
| 2 | (1, 0) | Движение мыши вправо = блок вправо (+X) |
| 3 | (0, 1) | Движение мыши вверх |
| 4 | (0.5, 0.5) | Движение мыши вправо-вверх |
| 5 | (-1, 0) | Движение мыши влево |

### Как работает:
```
1. Движение мыши → mouseDelta
2. mouseDelta · pushVector → накапливается в target
3. target = lerp(target, 0, friction) → затухание
4. current = lerp(current, target, ease) → интерполяция
5. mesh.position = originalPosition + current * 1.5

Stagger: каждый блок начинает с задержкой (0, 50, 100... 250ms)
```

### Мёртвая зона:
- 5% от центра экрана
- Блоки не реагируют на малые движения

### Ограничения:
```js
minOffset: -1.0  // Мин. смещение
maxOffset: 1.5   // Макс. смещение
```
Блоки не проходят сквозь друг друга.

### Смещение в покое:
```js
restOffset: (0, -2, 0)  // Все блоки опущены на -2 по Y
```

---

## 🎮 GUI Панели (расположение по углам)

### 🔷 Hexagons Control (левый верхний угол)
- Орбиты для 3 шестиугольников
- Интенсивность света
- Сохранение/загрузка настроек
- Сброс позиций

### G Material (правый верхний угол)
- Transmission параметры
- Цвет материала
- Поверхность (metalness, reflectivity...)
- Окружение

### Scene Lights (правый нижний угол)
- Ambient Light (color, intensity)
- Directional Light (color, intensity, position)
- Helpers toggle (axes, grid)

---

## 💾 Сохранение настроек

### SettingsManager:
- Сохранение в localStorage
- Экспорт в JS файл (hexagon-settings.js)
- Загрузка сохранённых настроек

### Команда для сброса localStorage:
```javascript
localStorage.clear()
location.reload()
```

---

## 📝 Команды

```bash
# Запуск
npm run dev

# Сборка
npm run build

# Preview
npm run preview
```

---

## 🎯 Итог

### Что работает:
- ✅ G Material с transmission (матовое стекло)
- ✅ 3 шестиугольника-источника с GUI
- ✅ Чёрные маркеры позиций
- ✅ Гидравлическая анимация блоков от мыши
- ✅ Stagger эффект (волна по блокам)
- ✅ Мёртвая зона 5%
- ✅ Ограничения от пересечений
- ✅ Сохранение настроек
- ✅ GUI панели по углам (не перекрывают)

### Параметры анимации:
- **Амплитуда**: 1.5x (очень мягко)
- **Stagger**: 0 → 250ms (плавная волна)
- **ease**: 0.08 (плавная интерполяция)
- **friction**: 0.03 (мягкое затухание)

---

## 📦 Зависимости (package.json)

```json
{
  "dependencies": {
    "three": "^0.183.0",
    "lil-gui": "^0.21.0",
    "gsap": "^3.12.0"
  }
}
```
